import os
import subprocess

# Variável para definir quantas vezes cada teste será executado
NUM_REPETICAO = 1


# Comandos a serem executados
commands = [
    {
        "description": "Executando cliente TCP via Docker Compose para enviar 10000 requisições INFO ao servidor 10000 10.5.0.2:8080, salvando logs em performance.log e resultados em resultados_linux.csv.",
        "command": "python3 main_script.py --compose_file compose-tcp.yml --service tcp-client --script tcp-client.py --host  10.5.0.2 --port 8080 --requests 15 --command INFO --verbose True --keep 0 --write_to_file 1",
    },
    {
        "description": "Executando cliente TCP via Docker Compose para enviar 15 requisições INFO ao servidor 15.5.0.2:8080 com modo detalhado (verbose), salvando logs em performance.log e resultados em resultados_linux.csv.",
        "command": "python3 main_script.py --compose_file compose-tcp.yml --service tcp-client --script tcp-client.py --host 10.5.0.2 --port 8080 --requests 15 --command INFO --verbose True --keep 1 --write_to_file 1",
    },
    {
        "description": "Executando cliente TCP via Docker Compose para enviar 15 requisições INFO ao servidor 15.5.0.2:8080, salvando logs em performance.log e resultados em resultados_linux.csv.",
        "command": "python3 main_script.py --compose_file compose-tcp.yml --service tcp-client --script tcp-client.py --host 10.5.0.2 --port 8080 --requests 15 --command INFO --keep 0 --write_to_file 1",
    },
    {
        "description": "Executando cliente TCP via Docker Compose para enviar 15 requisições INFO ao servidor 15.5.0.2:8080 com modo detalhado (verbose), salvando logs em performance.log e resultados em resultados_linux.csv.",
        "command": "python3 main_script.py --compose_file compose-tcp.yml --service tcp-client --script tcp-client.py --host 10.5.0.2 --port 8080 --requests 15 --command INFO ---keep 1 --write_to_file 1",
    },
    {
        "description": "Executando cliente TCP local diretamente (sem Docker) para enviar 15000 requisições INFO ao servidor local (127.0.0.1:8080), salvando logs em performance.log e resultados em resultados_linux.csv.",
        "command": "python3 main_script.py --script tcp-client/tcp-client.py --host 127.0.0.1 --port 8080 --requests 15 --command INFO --verbose True --keep 0 --write_to_file 1",
    },
    {
        "description": "Executando cliente TCP local diretamente (sem Docker) para enviar 15 requisições INFO ao servidor local (127.0.0.1:8080) com modo detalhado (verbose), salvando logs em performance.log e resultados em resultados_linux.csv.",
        "command": "python3 main_script.py --script tcp-client/tcp-client.py --host 127.0.0.1 --port 8080 --requests 15 --command INFO --verbose True --keep 1 --write_to_file 1",
    },
    {
        "description": "Executando cliente TCP local diretamente (sem Docker) para enviar 15 requisições INFO ao servidor local (127.0.0.1:8080), salvando logs em performance.log e resultados em resultados_linux.csv.",
        "command": "python3 main_script.py --script tcp-client/tcp-client.py --host 127.0.0.1 --port 8080 --requests 15 --command INFO --keep 0 --write_to_file 1",
    },
    {
        "description": "Executando cliente TCP local diretamente (sem Docker) para enviar 15 requisições INFO ao servidor local (127.0.0.1:8080) com modo detalhado (verbose), salvando logs em performance.log e resultados em resultados_linux.csv.",
        "command": "python3 main_script.py --script tcp-client/tcp-client.py --host 127.0.0.1 --port 8080 --requests 15 --command INFO  --keep 1 --write_to_file 1",
    },
]


def start_server():
    print("Iniciando servidor...")
    os.system("docker compose -f compose-server.yml up -d --remove-orphans")
    # os.system("python3 tcp-server/tcp-server.py")
    print("Servidor iniciado com sucesso.")

def start_docker_server():
    command = ["docker", "compose", "-f", "compose-server.yml", "run", "tcp-server"]
    subprocess.Popen(command, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
    print("Servidor iniciado em segundo plano.")

# Função para executar os comandos
def execute_commands():
    for i in range(NUM_REPETICAO):
        print(f"Execução {i+1} de {NUM_REPETICAO}:\n")
        for cmd in commands:
            start_server()
            print(cmd["description"])
            os.system(cmd["command"])
            print("\n")


# Execução principal
if __name__ == "__main__":
    # start_server()
    execute_commands()
    print("Todos os comandos foram executados com sucesso.")
